@model TestProject.Models.Classes
@{ 
    ViewBag.Title = "Удаление класса";
}

<div>
    <dl>
        <dt>Наименование</dt>
        <dd>
            @Html.DisplayFor(model=>model.ClassName)
        </dd>
    </dl>
    <div>
        <form asp-controller="Home" asp-action="Delete" asp-route-classname="@Model.ClassName">
            <div class="form-group">
                <input type="submit" value="Удалить" />
            </div>
        </form>
    </div>
</div>
